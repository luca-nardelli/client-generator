{{#each imports}}
import { {{type}} } from '{{file}}';
{{/each}}
{{#if imports.length}}

{{/if}}
export interface {{{title}}} {
  '@id'?: string;
{{#each fields}}
 {{{name}}}{{#if notrequired}}?{{/if}}: {{{type}}};
{{/each}}
}
